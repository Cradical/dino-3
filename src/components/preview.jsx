import React from 'react';

const Preview = ({ value }) => {
    return(
    <div>
        <section id="application-preview" className="">{ value }</section>
    </div>
    )
}

export default Preview